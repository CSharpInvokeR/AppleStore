<Window x:Class="AppleStore.Pages.ManagerPages.ChangeStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Изменение статуса заказа" Height="280" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False" Icon="/Resources/logo.png">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Заказ №" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox x:Name="txtOrderId" Grid.Row="1" Height="35" Margin="0,0,0,15" 
                 IsReadOnly="True" Background="#F0F0F0" VerticalContentAlignment="Center" Padding="10,0"/>

        <TextBlock Grid.Row="2" Text="Новый статус" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbStatus" Grid.Row="3" Height="35" Margin="0,0,0,20" 
                  VerticalContentAlignment="Center" Padding="5,0">
            <ComboBoxItem Content="Оплачен"/>
            <ComboBoxItem Content="Отправлен"/>
            <ComboBoxItem Content="Доставлен"/>
            <ComboBoxItem Content="Отменен"/>
        </ComboBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Height="35" Margin="0,0,20,0" 
                    Style="{StaticResource PrimaryButton}"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="35" 
                    Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>